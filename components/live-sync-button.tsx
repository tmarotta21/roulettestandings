/**
 * Phase 2: copy tournamentleagues LiveSyncButton.
 * POST /api/live with 15s cooldown + maybeRefresh (live ~45s, full ~5min).
 * Show on the public standings page.
 */
export function LiveSyncButton() {
  return null;
}
