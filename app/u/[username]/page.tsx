export default function UsernameGatePage() {
  return (
    <div className="space-y-3">
      <h1 className="text-2xl font-semibold">Username</h1>
      <p className="text-sm text-emerald-100/70">
        Phase 2: Sleeper username input (tournamentleagues FollowPicker + rotorooster
        `getUser` / `getUserLeagues`), then intersect with hosted roulette leagues.
      </p>
    </div>
  );
}
